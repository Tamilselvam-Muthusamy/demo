export enum Role {
  User = 'User',
  Admin = 'Admin',
}

export enum TaskStatus {
  ToDo = 1,
  InProgress = 2,
  Resolved = 3,
  Reopened = 4,
}

export enum TaskType {
  Task = 'Task',
  Bug = 'Bug',
}
